# Huayin Model Weights

The final v2Pro checkpoints are tracked with Git LFS:

- `huayin-e15.ckpt`: GPT semantic model
- `huayin_e8_s2536.pth`: SoVITS acoustic model

Install Git LFS before cloning or pulling the repository:

```bash
git lfs install
git lfs pull
```
